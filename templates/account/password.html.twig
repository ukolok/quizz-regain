{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe - Activ'Regain
{% endblock %}

{% block content %}
	<h1>Modifier mon mot de passe
	</h1>

	<hr>
	{% if notification %}
		<div class="alert alert-info">{{ notification }}</div>
	{% endif %}
	<a href="{{path('account')}}">Retourner à mon compte</a>
	<hr>
	{{ form(form)}}{% endblock %}
